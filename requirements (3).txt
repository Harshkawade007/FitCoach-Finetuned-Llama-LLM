# Core training stack (Colab T4)
transformers==5.8.1
trl==1.4.0
peft
bitsandbytes
accelerate
unsloth
datasets

# Data generation
openai            # DeepInfra's OpenAI-compatible API client
sentence-transformers  # all-MiniLM-L6-v2 deduplication

# App (see app/requirements.txt for the deployment-only subset)
gradio
torch
